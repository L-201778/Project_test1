# Copilot Instructions

## Variable Naming Convention

All variable names must use **ALL UPPERCASE** format (e.g., `MY_VARIABLE`, `USER_COUNT`, `MAX_VALUE`).

This rule applies to all variables across the entire codebase. Collaborators and GitHub Copilot must follow this naming convention when writing or generating code.
